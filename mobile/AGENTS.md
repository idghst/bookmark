# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

멀티 에이전트 레인, 계약, 커밋 규칙은 저장소 루트 `AGENTS.md`의 `mobile` 레인을 따른다. 이 디렉터리만 수정하고 웹 앱·API 저장소는 읽기만 한다.
